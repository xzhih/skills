interface:
  display_name: "Agent Debate"
  short_description: "Debate and prune requirement candidates"
  default_prompt: "Use $agent-debate on the same Core Problem and candidate set to keep, modify, cut, defer, or escalate each branch, returning a compact pruning ledger without a transcript."
policy:
  allow_implicit_invocation: false
