interface:
  display_name: "Project Context"
  short_description: "Restore source-of-truth project state"
  default_prompt: "Within an active workflow already routed to $project-context, recover only the authoritative state needed by the calling controller, then return a compact context packet and candidate next owner without switching controllers."
policy:
  allow_implicit_invocation: false
