interface:
  display_name: "Agent Spec"
  short_description: "Produce implementation-ready Specs"
  default_prompt: "Use $agent-spec to convert governed requirements into an implementation-ready Spec with B-* behavior IDs, requirement coverage, ambiguity/scope self-review, and review when behavior or boundaries need higher confidence."
policy:
  allow_implicit_invocation: false
