interface:
  display_name: "Agent Spec"
  short_description: "Produce implementation-ready Specs"
  default_prompt: "Use $agent-spec to convert governed requirements into an implementation-ready Spec with concrete behavior and coverage, then complete the mandatory independent whole-Spec review before Eval unless the user records an artifact-scoped waiver."
policy:
  allow_implicit_invocation: false
