interface:
  display_name: "Agent Lanes"
  short_description: "Batch subagent lane work safely"
  default_prompt: "Use $agent-lanes to split accepted plan tasks into non-overlapping lanes with coverage trace, owned surfaces, verification/evidence contracts, and structured handoffs; route same-topic debate to agent-debate and same-artifact review to agent-review."
policy:
  allow_implicit_invocation: false
