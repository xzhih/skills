interface:
  display_name: "Agent Review"
  short_description: "Review one artifact with agents"
  default_prompt: "Use $agent-review to have multiple agents review the same artifact, normalize findings, run rebuttal or recheck when needed, and return evidence-backed review status."
policy:
  allow_implicit_invocation: false
