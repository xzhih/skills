interface:
  display_name: "Agent Requirements Analysis"
  short_description: "Shape requirements with agents"
  default_prompt: "Use $agent-requirements-analysis to turn a direction into governed requirements with a question backlog, resolver matrix, confirmed/draft/open state, coverage trace, agent-resolved questions, and true user decisions before Spec."
policy:
  allow_implicit_invocation: false
