interface:
  display_name: "Integration Review"
  short_description: "Review returned lanes and evidence"
  default_prompt: "Within an active workflow already routed to $integration-review, normalize returned lane/worktree handoffs with owned surfaces, verify diffs/evidence, integrate each authorized ready lane, verify the combined target, record the merged transition, and return next-batch eligibility without dispatching it."
policy:
  allow_implicit_invocation: false
