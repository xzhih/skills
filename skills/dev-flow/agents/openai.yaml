interface:
  display_name: "Dev Flow"
  short_description: "Route development lifecycle work"
  default_prompt: "Use $dev-flow as the development lifecycle spine: restore context, route through requirements/spec/eval/plan with coverage trace, execute, review/test, repair/recheck, and coordinate doc-driven truth or agent workflows when needed."
policy:
  allow_implicit_invocation: false
