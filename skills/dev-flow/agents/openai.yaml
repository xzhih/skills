interface:
  display_name: "Dev Flow"
  short_description: "Route development lifecycle work"
  default_prompt: "Use $dev-flow to restore lifecycle state when needed, load the next owner skill, continue through its action, and close or hand off from current evidence."
policy:
  allow_implicit_invocation: false
