interface:
  display_name: "Agent Eval"
  short_description: "Define acceptance evidence"
  default_prompt: "Use $agent-eval to convert a locked Spec into E-* acceptance/evidence IDs, behavior coverage, automated/manual verification, failure conditions, and evidence requirements before planning."
policy:
  allow_implicit_invocation: false
