interface:
  display_name: "Agent Plan"
  short_description: "Plan execution and lane candidates"
  default_prompt: "Use $agent-plan to convert locked Spec and Eval artifacts into implementation tasks with coverage trace, files/interfaces, exact verification, evidence expectations, and safe lane candidates, then complete the mandatory independent whole-Plan review before execution or lanes unless an artifact-scoped waiver applies."
policy:
  allow_implicit_invocation: false
