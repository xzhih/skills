interface:
  display_name: "Multi-Agent Orchestration"
  short_description: "Drive Spec/Eval work with adversarial agents"
  default_prompt: >-
    Use $multi-agent-orchestration at the lightest sufficient intensity to
    coordinate Spec/Eval review, task-queue execution, adversarial repair, and
    evidence closure while treating agent reports as claims.
