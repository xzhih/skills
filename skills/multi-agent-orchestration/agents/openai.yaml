interface:
  display_name: "Multi-Agent Orchestration"
  short_description: "Drive heavy Spec/Eval agent workflows"
  default_prompt: >-
    Use $multi-agent-orchestration at the lightest sufficient intensity to
    coordinate Spec/Eval review, task-queue execution, adversarial repair,
    external-agent policy, and evidence closure while treating agent reports
    as claims.
