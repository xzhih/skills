interface:
  display_name: "Doc-Driven Workflows"
  short_description: "Build and maintain source-backed project docs"
  default_prompt: "Use $doc-driven-workflows to decide whether to bootstrap or maintain source-backed architecture, tech-stack, domain, operation-flow, call-path, and open-question docs, or skip without changing docs."
policy:
  allow_implicit_invocation: false
