interface:
  display_name: "Agent Grilling"
  short_description: "Pressure-test unclear goals with agents"
  default_prompt: "Within an active workflow already routed to $agent-grilling, pressure-test unclear goals with agents, resolve agent-answerable questions, and return decision candidates to the calling controller."
policy:
  allow_implicit_invocation: false
