interface:
  display_name: "Agent Grilling"
  short_description: "Grill assumptions to discover hidden gaps"
  default_prompt: "Use $agent-grilling within the active workflow to uncover hidden gaps through evidence and counterexamples, then return formulation or requirement deltas to the calling owner."
policy:
  allow_implicit_invocation: false
